#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    char str1[] = "theskyisturningredreturntopower";
    char str2[];
    char *rez = str1;
    int f = 0;
    int g = 1;
    int f, p, g, d;
    for (i = 1; s[i] != '\0'; i++)
    {
        if (f <= g)
        {
            if (s[i] != s[i - 1])
            {
                f = i + 1;
                p = i;
            }
            else
            {
                g = f;
                d = i;
            }
        }
    }
  printf("%s      %s",  g, d);
}
